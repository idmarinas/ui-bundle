{# @var attributes \Symfony\UX\TwigComponent\ComponentAttributes #}

{% props variant = 'notice' %}

{% if not enum('Idm\\Bundle\\Ui\\Twig\\Components\\Option\\MessageVariantEnum').isValidValue(variant) %}
	{% set variant = 'notice' %}
{% endif %}

{% set style = html_cva(
	base: 'flex items-center border-2 justify-center h-10 w-10 flex-shrink-0 rounded-full',
	variants: {
		type: {
			danger: 'bg-red-100 border-red-500',
			error: 'bg-red-100 border-red-500',
			success: 'bg-green-100 border-green-500',
			warning: 'bg-yellow-100 border-yellow-500',
			info: 'bg-blue-100 border-blue-500',
			notice: 'bg-gray-100 border-gray-500'
		}
	}
) %}

<div
	class="{{ style.apply({ type }, attributes.render('class'))|tailwind_merge }}"
	{{ attributes }}
>
	<twig:UX:Icon :name="'alert:' ~ variant" class="h-5 w-5" />
</div>
